{
	"version": "0.2.0",
	"configurations": [
        {
            "name": "Attach to Docker",
            "type": "Ruby",
            "request": "attach",
            "remotePort": "9000",
            "remoteHost": "localhost",
            "remoteWorkspaceRoot": "/opt/roadmap/",
            "cwd": "${workspaceRoot}"
        }
    ]
}
