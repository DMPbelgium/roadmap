#!/bin/bash

cd `dirname $0`/../..

[ -f env.sh ] && . env.sh

export RAILS_ENV=production

. ugent/bin/utils.sh

lock_file

orgid="$1"

if [ -z "$orgid" ];then
  echo "no orgid given" >&2
  exit 1
fi

bin/rails ugent:export:projects[$orgid]
