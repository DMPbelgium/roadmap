#!/bin/bash

if [ "$HOSTNAME" != "web.dmpprod.dmponline.be" ];then
    echo "no need to this script outside of the production server" >&2
    exit
fi

cd `dirname $0`/../..

[ -f env.sh ] && . env.sh

export RAILS_ENV=production

. ugent/bin/utils.sh

lock_file

bin/rails ugent:cleanup:projects
